/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "i.ibb.co", 
            "hola.syscomatic.com",
            "hola.syscomatic.comnull",
        ],
    },
};

export default nextConfig;
